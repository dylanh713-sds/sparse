# Sparse Cross Product

Computes the inner product between two sparse vectors.

## Usage

``` r
sparse_crossprod(x, y, ...)

# S4 method for class 'sparse_numeric,sparse_numeric'
sparse_crossprod(x, y, ...)
```

## Arguments

- x, y:

  Objects of class `sparse_numeric`

- ...:

  Additional arguments

## Methods (by class)

- `sparse_crossprod(x = sparse_numeric, y = sparse_numeric)`: Sparse dot
  product
