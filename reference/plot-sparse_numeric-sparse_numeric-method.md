# Plot Two Sparse Numeric Vectors

Generates an Rplot which shows points at which two vectors are non-zero,
and their values.

## Usage

``` r
# S4 method for class 'sparse_numeric,sparse_numeric'
plot(x, y, ...)
```

## Arguments

- x, y:

  sparse_numeric objects

- ...:

  extra arguments
