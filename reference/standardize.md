# Standardize a Sparse Numeric Vector

Z-score standardizes a sparse numeric vector

## Usage

``` r
standardize(x, ...)

# S4 method for class 'sparse_numeric'
standardize(x, ...)
```

## Arguments

- x:

  A sparse_numeric object

- ...:

  Additional arguments

## Methods (by class)

- `standardize(sparse_numeric)`: Vector standardization
