# Sparse vector norm

Computes the Euclidean norm of a sparse numeric vector

## Usage

``` r
norm(x, ...)

# S4 method for class 'sparse_numeric'
norm(x, ...)
```

## Arguments

- x:

  A sparse_numeric object

- ...:

  Additional arguments

## Methods (by class)

- `norm(sparse_numeric)`: Euclidean norm
