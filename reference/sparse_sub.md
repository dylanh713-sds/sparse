# Subtract Two Sparse Vectors

Subtract Two Sparse Vectors

## Usage

``` r
sparse_sub(x, y, ...)

# S4 method for class 'sparse_numeric,sparse_numeric'
sparse_sub(x, y, ...)
```

## Arguments

- x, y:

  Objects of class `sparse_numeric`

- ...:

  Additional arguments

## Methods (by class)

- `sparse_sub(x = sparse_numeric, y = sparse_numeric)`: Subtract two
  sparse vectors
